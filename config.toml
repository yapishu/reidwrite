baseurl = "https://artorias.pw" # Controls base URL
languageCode = "en-US" # Controls html lang attribute
title = "reid/write" # Homepage title and page title suffix
paginate = 15 # Number of posts to show before paginating
theme = "after-dark"
enableEmoji = true

enableRobotsTXT = true # Suggested, enable robots.txt file
googleAnalytics = "" # Optional, add tracking Id for analytics
disqusShortname = "reidwrite" # Optional, add Disqus shortname for comments
SectionPagesMenu = "main" # Enable menu system for lazy bloggers
footnoteReturnLinkContents = "↩" # Provides a nicer footnote return link

[params]
  description = "" # Suggested, controls default description meta
  author = "" # Optional, controls author name display on posts
  hide_author = true # Optional, set true to hide author name on posts
  show_menu = false # Optional, set true to enable section menu
  powered_by = false # Optional, set false to disable credits
  images = [] # Suggested, adds default OpenGraph image

[menu]
	[[menu.main]]
	name = "home"
	weight = 0
	identifier = "home"
	url = "/"
	[[menu.main]]
	name = "about"
	weight = 1
	identifier = "about"
	url = "/about"
  enableEmoji = true
